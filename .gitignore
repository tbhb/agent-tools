# Local development
tmp/
.env
.env.*
!.env.example
mise.local.toml

# Agent commit-message draft. Authored at the repo root so vale's
# [formats] mapping resolves it, linted via `just lint-commit-msg`,
# then consumed with `git commit -F COMMIT_AGENTMSG`. Never committed.
COMMIT_AGENTMSG

# Agent pull request drafts, the same idea one stage later.
# PR_AGENTDESC.md holds the description for as long as the pull request
# stays open, and merge-pr removes it once the branch merges.
# SQUASH_AGENTMSG holds the squash commit message that skill writes.
# Linted via `just lint-pr-description` and `just lint-squash-msg`.
PR_AGENTDESC.md
SQUASH_AGENTMSG

# Go build output
bin/
/agentcontext
/agenthooks
/agentstore
coverage.out
coverage.html
coverage.txt
coverage.xml
coverage.covdata*/

# Test outputs
testdata/fuzz/

# Python
__pycache__/
*.py[co]
.venv/
.pytest_cache/
.hypothesis/
.coverage
.coverage.*
htmlcov/

# cosmic-ray mutation session databases. The `mutate-py` recipes write
# the work order plus per-mutant results here; it is regenerated each
# run.
.cosmic-ray/

# Linter caches
.rumdl_cache/
node_modules/
.cspell-cache
.ruff_cache/
.complexipy_cache/

# goreleaser
dist/

# gremlins
.gremlins/
gremlins-report.json

# Claude Code
.claude/worktrees/
.claude/settings.local.json

# Vale synced packages (keep the repo-local style and its vocabulary).
# Each negation needs the parent un-ignored first — git can't re-include
# a child whose ancestor directory stays excluded. The agent output
# template is no longer among the tracked files: the ai-tells package
# ships it and vale sync writes it under .vale/config/templates/.
.vale/*
!.vale/project/
!.vale/config/
.vale/config/*
!.vale/config/vocabularies/
.vale/config/vocabularies/*
!.vale/config/vocabularies/project/

# macOS
.DS_Store

# APM dependencies
apm_modules/

# Session retrospectives. Local working notes, never committed.
.claude/retros/
